# Глава 5: Интерфейсы и протоколы связи - Документация

## Краткий обзор

Глава 5 знакомит с реальными протоколами связи и интерфейсами, используемыми в современных цифровых системах:

- **I2C протокол** для работы с датчиком температуры TMP102
- **PDM (Pulse Density Modulation)** для аудио захвата и воспроизведения
- **Цифровая обработка сигналов** и сглаживание данных
- **Open-drain интерфейсы** и timing требования

## Проекты в главе

### 🌡️ i2c_temp.sv - Датчик температуры
- Реализация I2C master контроллера
- Сглаживание данных с параметрическими фильтрами
- Преобразование в BCD для семисегментного дисплея
- Timing-критичный автомат состояний

### 🎵 PDM Audio System - Аудиозапись
- PDM демодуляция для захвата звука с микрофона
- Delta-Sigma модулятор для воспроизведения
- Буферизация в BRAM памяти
- Визуальная индикация уровня сигнала

## Ключевые файлы

| Файл | Назначение | Сложность |
|------|------------|-----------|
| `temp_pkg.sv` | Пакет с BCD функциями | Базовая |
| `i2c_temp.sv` | I2C контроллер температуры | Высокая |
| `pdm_top.sv` | Верхний уровень аудиосистемы | Средняя |
| `pdm_inputs.sv` | PDM демодулятор | Средняя |
| `pdm_output.sv` | Delta-Sigma модулятор | Базовая |

## Изученные концепции

### Протоколы связи
- **I2C timing диаграммы** и автоматы состояний
- **Open-drain сигналы** и pull-up резисторы
- **Master-Slave архитектуры** и арбитраж шины

### Цифровая обработка
- **PDM модуляция/демодуляция** принципы
- **Скользящее среднее** для сглаживания
- **FIFO буферизация** и управление памятью

### Практические аспекты
- **Timing constraints** для протоколов
- **Mark_debug атрибуты** для отладки протоколов
- **Параметрическое программирование** интерфейсов

## Тестирование

- `tb_temp.sv` - симуляция I2C с ускоренными параметрами
- `tb_pdm.sv` - генератор синусоидального PDM сигнала для тестирования

## Связь с предыдущими главами

**Использует из Главы 4:**
- Конечные автоматы для протоколов
- Timing-критичную логику состояний

**Использует из Главы 3:**
- Семисегментные дисплеи
- Подавление дребезга кнопок

**Использует из Главы 2:**
- BCD арифметику и преобразования
- Параметрические модули

## Следующие шаги

После изучения этой главы вы готовы к:
- Реализации других протоколов (SPI, UART)
- Продвинутой цифровой обработке сигналов
- Интеграции с реальными устройствами
- Созданию custom протоколов связи